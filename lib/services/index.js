const TelegramBot = require('./telegram-bot');
const PingService = require('./ping-service');
const PingServiceManager = require('./ping-service-manager');

module.exports = {
  TelegramBot,
  PingService,
  PingServiceManager,
};
